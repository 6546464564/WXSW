<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/itemRoot"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_columnWeight="1"
    android:layout_margin="4dp"
    android:background="?selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_horizontal"
    android:orientation="vertical">

    <!-- 封面 + 角标 (右上 + 左下), 叠层 -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:cardBackgroundColor="@color/wanxiang_card"
            app:cardCornerRadius="6dp"
            app:cardElevation="2dp">

            <ImageView
                android:id="@+id/ivCover"
                android:layout_width="match_parent"
                android:layout_height="135dp"
                android:background="@drawable/bs_cover_placeholder"
                android:contentDescription="@null"
                android:scaleType="centerCrop" />
        </androidx.cardview.widget.CardView>

        <!-- 万象书屋: 封面右上角徽章 (必读 / 会员) -->
        <TextView
            android:id="@+id/tvBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end|top"
            android:background="@drawable/bs_badge_hot"
            android:includeFontPadding="false"
            android:paddingHorizontal="6dp"
            android:paddingVertical="2dp"
            android:textColor="#FFFFFF"
            android:textSize="10sp"
            android:textStyle="bold"
            android:visibility="gone"
            tools:text="必读"
            tools:visibility="visible" />

        <!-- 万象书屋: 封面左下角来源小标签 (类似 QQ 阅读「起点读书」) -->
        <TextView
            android:id="@+id/tvSource"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start|bottom"
            android:background="@drawable/bs_source_chip"
            android:includeFontPadding="false"
            android:paddingHorizontal="5dp"
            android:paddingVertical="1dp"
            android:text="@string/bs_source_label"
            android:textColor="#FFFFFF"
            android:textSize="9sp"
            android:textStyle="bold"
            tools:ignore="SmallSp" />
    </FrameLayout>

    <TextView
        android:id="@+id/tvName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:ellipsize="end"
        android:gravity="start"
        android:maxLines="2"
        android:minLines="2"
        android:textColor="@color/wanxiang_text_primary"
        android:textSize="14sp"
        android:textStyle="bold"
        tools:text="一本书的名字" />

    <!-- 万象书屋 D-22: 真作者. 数据源 m.qidian.com 提供 bAuth, 显示在书名下方一行. -->
    <TextView
        android:id="@+id/tvAuthor"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:ellipsize="end"
        android:gravity="start"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:textColor="@color/wanxiang_text_secondary"
        android:textSize="11sp"
        android:visibility="gone"
        tools:text="爱潜水的乌贼"
        tools:visibility="visible" />

    <!-- 万象书屋: 书名下方副标签 -->
    <TextView
        android:id="@+id/tvTag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:layout_marginTop="4dp"
        android:background="@drawable/bs_tag_chip"
        android:ellipsize="end"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:paddingHorizontal="6dp"
        android:paddingVertical="2dp"
        android:textColor="@color/wanxiang_text_secondary"
        android:textSize="11sp"
        android:visibility="gone"
        tools:text="奇闻怪诞"
        tools:visibility="visible" />
</LinearLayout>
