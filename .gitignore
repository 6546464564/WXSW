*.iml
.DS_Store

# ─── 全局 build / 缓存 ───────────────────────────────────
build/
/build
node_modules/
package-lock.json
.idea/
.externalNativeBuild
/tmp
/release
/captures

# ─── Android (现在统一在 android/ 下) ────────────────────
android/.gradle/
android/.kotlin/
android/build/
android/local.properties
android/app/build/
android/app/app/
android/app/google/
android/app/gradle.properties
android/modules/*/build/

# Kotlin 2.0
.kotlin/
.gradle

# 旧布局兼容 (历史 commit 仍可能引用)
/local.properties
/app/build/
/app/app/
/app/google/
/app/gradle.properties

# ─── iOS ────────────────────────────────────────────────
ios/.build/
ios/BookSourceCLI/.build/
ios/**/.build/
ios/build/
ios/**/*.xcuserstate
ios/**/xcuserdata/
ios/**/DerivedData/

# ─── scratch / 反编译参考 (本地调试产物, 不入库) ─────────
scratch/
.tools/

# ─── 审计 / 临时调试工件 (本机产物, 含 cookie / 截图) ────
.audit-tmp/
.audit-*.png
.audit-*.json

# ─── 本地 IDE / AI 工具配置 ──────────────────────────────
.claude/
.cursor/
.vscode/
*.log

# ─── Backend 本地数据 / 环境 ─────────────────────────────
backend/data/
backend/.env
backend/*.db
backend/*.db-journal
backend/*.db-wal
backend/*.db-shm

# ─── Android 签名 / 私钥 (绝不能进 git!!!) ───────────────
# 历史教训: 早期 wanxiang.keystore 曾被 commit + 弱密码 'wanxiang',
# 任何 clone 仓库的人都能伪造 App 升级. 现在所有 keystore + 密码备忘
# 文件 + 旧 keystore 备份 都强制忽略.
android/keystore/*.keystore
android/keystore/*.jks
android/keystore/.local-password.txt
android/keystore/*.OLD*
keystore/*.keystore
keystore/*.jks

# 万象书屋: 联调辅助脚本 (Cursor 生成, 不入仓库)
install-and-run.sh

# 万象书屋: xcodegen 临时备份文件
ios/project.yml.bak
